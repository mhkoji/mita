#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.server.3836709310
  (:use :cl))
(in-package :ros.script.server.3836709310)
(ql:quickload :mita-gui-backend)

;; In oreder not to install ssl on Windows.
(push :hunchentoot-no-ssl *features*)

(defun main (&rest argv)
  (mita.gui.ws.server:start :use-thread nil)
  (declare (ignorable argv)))
;;; vim: set ft=lisp lisp:
