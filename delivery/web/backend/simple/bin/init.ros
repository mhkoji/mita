#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.init.3836755732
  (:use :cl))
(in-package :ros.script.init.3836755732)
(ql:quickload :mita-delivery-web-simple)

(defun main (&rest argv)
  (declare (ignorable argv))
  (mita.delivery.web.simple:init))
   
;;; vim: set ft=lisp lisp:
