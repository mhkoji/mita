#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.server.3836752160
  (:use :cl))
(in-package :ros.script.server.3836752160)
(ql:quickload :mita-delivery-web-simple)

(defun main (&rest argv)
  (declare (ignorable argv))
  (mita.delivery.web.simple:start
   :use-thread nil))

;;; vim: set ft=lisp lisp:
